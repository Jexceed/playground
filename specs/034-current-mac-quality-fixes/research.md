# 研究决策
- 图集错误来自父选择器作用于atlas内部img；限定普通图与图集容器各自尺寸即可，保留现有图像资产。
- 多步合并复用格位响应而非新建答案协议；以独立折叠/集合运算测试确保所有中间格正确。三角形展示是布局元数据，不制造虚假空格。
- 状态消息共用预留区域；亲子活动按任务阶段显示，避免把字缩小或隐藏必要操作。
- 内容改动与负荷表依据现任务和032反例；独立研究输出在research/，将作为验证输入。没有新依赖或外部技术选型。
- 实测年龄适配取决于真实孩子反馈；当前只产出可审计设计估计。亲子方案不统一假装三阶递进。
