# 决策
- 图片变大优先利用卡片内和辅助行的空间；整体页面不缩放。对照方案是只加放大弹窗，这无法改善默认阅读。
- 同时提供独立大图查看，使用项目已经采用的原生dialog与ActivityImage。避免另画图片、整张图集显示或引入新弹窗依赖。
- 图卡按钮与放大按钮并列，放大不触发选卡、填槽、提示或尝试事件。记忆阶段改变关闭弹窗，避免延长隐藏前的图片暴露。
- 先实现并量测具体尺寸，再按当前Mac真实窗口调整；不扩展到新的多端设计工作。
