# 数据
没有题目语义、持久化或资源格式变化。图片框和名称由普通容器排版，一个覆盖整卡的语义按钮接收输入；选中/使用状态仍由同一已有状态导出，aria-pressed保留。框外名称不新增独立选择状态。图片不能再依赖原生button内部百分比宽度计算。

storySequence增加可选tokenIds，必须恰好覆盖活动全部图卡且无重复。L06第7～9题用它把已给定的乱序直接显示为候选；同一顺序也用于大图切换，不再另绘一份重复参照图。
