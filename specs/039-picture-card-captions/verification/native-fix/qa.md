# WebKit图卡收缩回归

2026-09-26，基线1696c07。

## 原生复现
本轮最初成功读取/Applications/小小思考屋.app的AX和原生截图，复现用户报告：E03第1题的三幅图很小，短名称“开始下雨”的图框比六字名称更窄；放大按钮留在整列边缘。原实现把百分比宽度的图片框放在button内部，WebKit内部内容宽度跟随名称收缩。此前30页浏览器检查不能证明这条原生渲染路径正确。

## 修改
图片框和说明放进普通布局容器，明确188px宽并以列宽为上限；语义button不再包含视觉内容，绝对定位覆盖整张卡以接收点击/键盘/拖放。放大按钮仍为独立兄弟控件。按钮、图框和名字不再互相决定宽度，选择标记仍由原selected状态导出。

## 当前证据
- mac:install成功，包含最终tsc/Vite、Tauri打包、签名及安装。构建/安装二进制一致，严格签名通过，见install.json；新哈希57d068e28c88ef88a759ab8cb6028856dec03f00a29a0cf614876a2a767f9455。
- 90项回归和课程/插画/语音媒体审计通过；没有修改题库、图片文件或台词。
- CUA浏览器仅作为输入回归：三张短/长名称的图片全部178px，选中和拖放后相同；覆盖按钮和框均188px。点击说明实际坐标可以选择；真实指针拖放可填入位置。四图行图片全部148.75px，覆盖区域与边框均158.75px。图片不在button内，页面高度788。
- **新包原生默认显示已确认。** 用户继续后，通过完整Applications路径打开应用，观察到启动页、随后的E03第1题AX状态及2560×1640原生截图。三张不同名称长度的图片均恢复为相同的大图框，放大按钮归位到图框内，名称位于框外；题干、排序格、主操作和页脚都在窗口中。这个结论来自实际.app，不是浏览器。
- 获取默认原生截图后，继续检查原生选择/拖放时工具再次报告锁屏。**新包的原生选中/拖放/四图状态仍未实测**，只有上述浏览器输入回归通过；不把它们列为原生通过。

本轮按用户已有授权同步dev开发检查点，保留这些原生输入检查为明确待办；这次同步不表示035～039所有原生交互都已完成。待解锁后检查E03第1/6题选中/取消、名称点击、拖放和放大，再关闭T009及其他原生待办。
