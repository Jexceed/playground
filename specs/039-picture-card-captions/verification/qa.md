# 大图与框外说明验收

2026-09-26；基线c5d8f19；实际分支dev。

## 展示与交互

- 候选按钮的边框改到图片容器上，名称在边框下方，但仍是同一个可点击/键盘/拖动按钮。三图行边框188px、完整图片178px，原图132px；四图行边框158.75px、完整图片148.75px。图片只留3px内边距，名称14px，位于边框下6px。
- 保留准确图集分帧及原图片，不用裁掉画面内容来填框。选择/已使用状态应用到图片边框，不把框外文字重新包进带底色的大卡片。
- L06第7～9题原并排参照/候选造成图片约89px宽。现在将给定乱序以tokenIds显式绑定为候选顺序，去掉重复图列；图片统一约149px。大图浏览按当前候选次序，原先后线索和答案保留。

## CUA浏览器

当前Mac1280×820窗口对应1280×788内容区。E03/P02/L06各9题，加A05第4/5/6题，共30题；最终页面高度均788、scrollY为0、可见图片缺失0。所有可见候选名称的顶部都在对应图片框底部之后，图片保持正方形。没有新加CSS镜像测试。

- E03第1题：点击“天上积起乌云”的框外名称后选中；打开其大图、Escape返回后仍选中。随后通过真实CUA指针拖到第1个位置，填入成功，尝试数仍0。
- 通过Enter键选择“开始下雨”并放到第3格，再放入另一张形成错误顺序。错误反馈和提示均同屏；清空两个位置并改成正确顺序后判对。部分摆放、已用候选、错误、提示和完成状态没有溢出。
- E03、P02、L06完整题组逐页检查通过。L06第9题只显示一组原始乱序候选：step3→step2→step1→step0；“一起品尝”大图为1/4，向右切换到“把果块装碗”2/4。按洗净→切块→装碗→品尝填完，提示和完成态同屏并判对。
- A05第4/5/6题实际播放必听音频，ready/listening期间候选为空，结束后才出现178px图卡和框外名称；六格听力图集、种植图集分帧正常，页面均同屏。

## 工程验证

- 90项相关回归通过，课程、插画与语音媒体审计均0问题。
- 通过只读基线比较脚本[check-semantics.mjs](check-semantics.mjs)确认621道探索题除图片/表示之外所有字段不变，启蒙数据完全相同，4003条语音脚本完全相同；没有重做未改变的语音或图片。见[semantic-invariants.json](semantic-invariants.json)。
- 15图集/72帧均有实际引用。使用清单减少的12条记录是L06不再重复绘制的参照位置；原图文件和候选图引用保留。
- mac:install成功，包括最终tsc/Vite构建、Tauri打包及标准Applications安装；构建与安装二进制一致、严格签名校验通过，哈希见[result.json](result.json)。
- 安装后通过CUA访问原生应用仍收到Mac锁屏提示。原生复核与dev推送待手动解锁；此前请求尚待回复。浏览器结果不冒充原生检查。

解锁后应退出并重新打开/Applications/小小思考屋.app，复核三/四图布局、名称点击、拖放、独立放大与L06原始乱序；同时收尾035～038已记录的原生门禁。
