# G06 叠合、求同、求异与减去

本题族共9个原创任务，来源用于规则和能力对标，不逐页复制试卷。按三个台阶、每阶三个有效变式组织。

来源：S067 / 16-21；S067 / 28-30；S067 / 68-70；S105 / slide22；S118 / 17-20。原材料的阅读深度和缺失音频仍以review-ledger.json为准。运行内容不依赖原始下载文件。

数据：`src/curriculum/exploration/graphic.ts`。几何PNG由同一绘制模型生成，跨引擎浮点坐标先规范化。

| 活动 | 台阶 | 操作 | 题目 | 作者解与核对依据 |
|---|---|---|---|---|
| explore-shape-set-operations-1 | 1 | singleChoice | 按“并起来”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-2 | 1 | singleChoice | 按“并起来”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-3 | 1 | singleChoice | 按“并起来”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-4 | 2 | singleChoice | 按“共同”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-5 | 2 | singleChoice | 按“不同”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-6 | 2 | singleChoice | 按“共同”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-7 | 3 | singleChoice | 按“左减右”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-8 | 3 | singleChoice | 按“右减左”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |
| explore-shape-set-operations-9 | 3 | singleChoice | 按“左减右”的规则，结果是哪张？ | 逐格比较，再把符合规则的格子留下。 |

完整结构化答案见[full-answer-audit.json](full-answer-audit.json)。自动判题以集合、顺序、图约束或覆盖关系验证；亲子活动记录观察，不判成自动正确。每题的错误反馈、两级提示和亲子追问在同一活动定义中。图约束/拼搭另外由独立枚举/搜索检查合法替代解；数量关系有独立运算复算。

真实孩子的理解、操作负荷和年龄难度尚未校准，不能以成人或自动化测试代替。
