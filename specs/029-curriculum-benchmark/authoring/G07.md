# G07 图形金字塔与递归组合

本题族共9个原创任务，来源用于规则和能力对标，不逐页复制试卷。按三个台阶、每阶三个有效变式组织。

来源：S067 / 81；S067 / 84；S074 / 75；S074 / 78。原材料的阅读深度和缺失音频仍以review-ledger.json为准。运行内容不依赖原始下载文件。

数据：`src/curriculum/exploration/graphic.ts`。几何PNG由同一绘制模型生成，跨引擎浮点坐标先规范化。

| 活动 | 台阶 | 操作 | 题目 | 作者解与核对依据 |
|---|---|---|---|---|
| explore-recursive-composition-1 | 1 | singleChoice | 两块合成上面一块，结果是哪张？ | 任一块有色就留下。 |
| explore-recursive-composition-2 | 1 | singleChoice | 两块合成上面一块，结果是哪张？ | 任一块有色就留下。 |
| explore-recursive-composition-3 | 1 | singleChoice | 两块合成上面一块，结果是哪张？ | 任一块有色就留下。 |
| explore-recursive-composition-4 | 2 | singleChoice | 相邻两块先合成一块，再把中层合成顶层。 | 先算两组相邻图案，再用同一规则算顶层，不能直接跳过中层。 |
| explore-recursive-composition-5 | 2 | singleChoice | 相邻两块先合成一块，再把中层合成顶层。 | 先算两组相邻图案，再用同一规则算顶层，不能直接跳过中层。 |
| explore-recursive-composition-6 | 2 | singleChoice | 相邻两块先合成一块，再把中层合成顶层。 | 先算两组相邻图案，再用同一规则算顶层，不能直接跳过中层。 |
| explore-recursive-composition-7 | 3 | singleChoice | 相邻两块先合成一块，再把中层合成顶层。 | 先算两组相邻图案，再用同一规则算顶层，不能直接跳过中层。 |
| explore-recursive-composition-8 | 3 | singleChoice | 相邻两块先合成一块，再把中层合成顶层。 | 先算两组相邻图案，再用同一规则算顶层，不能直接跳过中层。 |
| explore-recursive-composition-9 | 3 | singleChoice | 相邻两块先合成一块，再把中层合成顶层。 | 先算两组相邻图案，再用同一规则算顶层，不能直接跳过中层。 |

完整结构化答案见[full-answer-audit.json](full-answer-audit.json)。自动判题以集合、顺序、图约束或覆盖关系验证；亲子活动记录观察，不判成自动正确。每题的错误反馈、两级提示和亲子追问在同一活动定义中。图约束/拼搭另外由独立枚举/搜索检查合法替代解；数量关系有独立运算复算。

真实孩子的理解、操作负荷和年龄难度尚未校准，不能以成人或自动化测试代替。
