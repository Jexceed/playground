# Matrix Viewport Audit Contract

`pnpm audit:curriculum` reports the following finding when a matrix round stacks
a separate scene illustration:

```text
logic-matrix-puzzle/<round-id>: matrix puzzle should use the matrix as its only evidence surface
```

Acceptance requires no finding with this message and no other curriculum
problems.
